[package]
name = "processing_instructions"
version = "0.1.0"
description = "An example program demonstrating instructions in Poseidon."
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "processing_instructions"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.30.1"
