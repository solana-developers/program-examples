[package]
name = "counter-mpl-stack"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[features]
no-entrypoint = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
borsh = "0.9"
shank = "0.0.8"
solana-program = "=1.18.17"
