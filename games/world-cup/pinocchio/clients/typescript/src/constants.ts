import { WORLD_CUP_PROGRAM_ADDRESS } from './generated/index.js';

/** Deployed program address, sourced from Codama-generated bindings. */
export const PROGRAM_ID = WORLD_CUP_PROGRAM_ADDRESS;

/** PDA seed for the event authority (self-CPI event emission). */
export const EVENT_AUTHORITY_SEED = 'event_authority';
